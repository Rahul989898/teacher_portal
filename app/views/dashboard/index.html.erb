<h1>Students List</h1>

<% if @students.any? %>
  <ul>
    <% @students.each do |student| %>
      <li><%= student.name %></li> <!-- Adjust the attribute as necessary -->
    <% end %>
  </ul>
<% else %>
  <p>No students found.</p>
<% end %>
